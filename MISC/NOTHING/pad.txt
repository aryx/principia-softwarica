this is an empty directory for codegraph/indexer/... to use as a parameter
with another directory so plan9/ is found as the root of the project
and pfff_macros.h at this root is used to parse the code.
