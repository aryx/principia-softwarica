TOP=../..
<$TOP/mkfiles/$objtype/mkfile

# claude: con -> con_ since mtools/mcopy (used by `mk disk`) refuses to
# create a file named after the reserved MS-DOS device name CON on a
# FAT image; source and man page renamed to match, see mk disk's diag.
TARG=con_\
	xms\
	xmr\
	hayes\

<$TOP/mkfiles/mkmany
