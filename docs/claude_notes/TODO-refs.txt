TODO: %ref: academic references next to the code, across Principia books
=========================================================================

Status: CANDIDATES ONLY -- nothing inserted in any .nw yet (2026-09-18).
Idea: like ~/playground's .mli "Reference:" comments, put the classic
(old, often forgotten) paper next to the chunk that implements the idea,
with a parenthetical that teaches: the paper's original problem/machine,
which function here uses the idea, and where Plan 9 differs.

Format (on the lines just before <<chunk>>=, or before a prose paragraph
in Intro/Overview principles sections):

  %ref: J. E. Bresenham, "Algorithm for computer control of a digital
  %  plotter", IBM Systems Journal 4(1):25-30, 1965 (for a pen plotter,
  %  ...; road not taken: memdraw fills even thin lines as polygons).

TODO: add %ref: to docs/latex/Tags.tex once the format is approved.
Later: promote the best ones to \cite{} + Principia.bib entries.

Candidate files (research brief: refs/BRIEF.md)
-----------------------------------------------
                                entries  %ref lines  medium-confidence
refs/asm_linker.txt                 32        48          5
refs/compiler.txt                   42        57          3
refs/kernel.txt                     37        49          4
refs/kernel_arch_machine.txt        42        55          9
refs/graphics.txt                   31        46          4
refs/windows_widgets_browser.txt    54        74          2
refs/libcore_network.txt            53        76          3
refs/tools.txt (mk rc ed utils      80       101         10
   debuggers profiler VCS)
                                   ---       ---         --
                                   371       506         40

Entries marked "CONFIDENCE: medium" have a detail (venue, pages, year,
attribution) quoted from memory: check or drop before inserting.

Caveats
-------
- Kernel_x86.nw is #include'd commented-out (Kernel.nw:48982): its
  refs won't render. Assembler_x86.nw got 0 entries for the same reason.
- Duplicates across books to resolve (one home + cross-ref):
  Wheeler 1952 (assemblers/Intro + Linker.nw ATEXT chunk),
  Killian 1984 "Processes as files" (Kernel procdir + Debugger attachproc),
  Thompson 1968 regex (Libcore regexec1 + Utilities grep + ed),
  Dijkstra 1960 "Recursive programming" (Assembler ARET + Linker + compilers/Intro),
  Pike "Getting dot-dot right" (Kernel Path + Libcore cleanname),
  Knuth hashing / UTF-8 / LZ77 (tools.txt flags them as maybe-Libcore).

Prose errors found in existing text -- ALL FIXED in the .nw (2026-09-18,
uncommitted; stats.c left for syncweb). Unverified ones were checked first.
--------------------------------------------------------------------
[V] = verified by me in the source; [ ] = agent report, not re-checked.
[V] Libcore.nw ~1838: qsort "recurses on the smaller partition, O(log n)";
    code recurses on the LARGER side (if(j >= n) qsorts(a, j, p)).
[ ] Libcore.nw ~4027/4154: pool free tree called "balanced"; never rebalanced.
[ ] Network.nw ~8533: balancetree "AVL-style"; single rotations only.
[V] Graphics.nw ~15834, ~16651: image compression called "run-length";
    libdraw/compressed.c is LZ77-style (hash chains, back-references).
    Agent also says the byte-code description is backwards.
[V] Graphics.nw Lines: memdraw has no Bresenham, slanted lines are filled
    as polygons; line.c:132 rejects Sutherland-Cohen clipping.
[ ] Graphics.nw ~925: pike-bitbltd-1984.pdf called "Bitblt Considered
    Harmful"; it is the SIGGRAPH'84 "Bitmap Graphics" course notes.
[V] Compiler.nw ~24286: regopt diagram "loops weight 5x"; LOOP is 3 (5c/gc.h:269).
[V] compilers/Intro.nw ~942: "per-basic-block allocation"; regopt is global.
[ ] compilers/Intro.nw ~767: "BURG, Aho and Johnson 1976" mixes two works.
[ ] compilers/Intro.nw: pointer arithmetic as C's innovation ignores BCPL.
[ ] Profiler.nw ~390: gprof "the reason we have the term profile"; Knuth 1971 used it.
[ ] Utilities.nw ~8400: dc credited to Cherry alone; V7 docs say Morris & Cherry.
[ ] Widgets.nw ~525: "Rob Pike carried [Tk's packer] into libpanel";
    libpanel is Tom Duff's (the book says so itself at ~193).
[V] Widgets.nw ~605: Dear ImGui attributed to Casey Muratori; it is Omar
    Cornut's (2014); Muratori coined "IMGUI" (2005).
[V] Browser.nw ~5762 (and ~5585): says RFC 3986 5.2 step 6; the code
    comment (~5793) says RFC 2396, and 3986 has no step 6.
[ ] Browser.nw ~10231, ~1189: cookie matching described as RFC 6265; code is RFC 2109.
[ ] Browser.nw ~8331: "mothra defers to the kernel's factotum"; it's webfs,
    and factotum is user-level. Section title mentions digest; only Basic exists.
[V] machine/5i/stats.c:207 (Machine.nw iprofile): qsort(prof,...) should be
    qsort(aprof,...) -- real code bug.

Bibliography issues
-------------------
- \cite'd but undefined: porter-duff, gprof, pcc, network-plan9,
  aho-weinberger-kernighan, r4rs, s9book, ssa, turing-wikipedia.
- pike-layers venue: bib says SIGGRAPH; Blit paper refs say ACM TOG 2(2):135-160.
- asl is Salomon 1993 (not Barron).
- csp is the 1985 book, not the 1978 CACM paper.
- window-system-should-be-transparent: page conflict (see windows file).
- build-alacarte defined but never cited.
- ~150+ cited works in the candidate files have no bib key yet (each file lists them).
